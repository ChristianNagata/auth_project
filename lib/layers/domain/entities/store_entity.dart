class StoreEntity {
  String categoria, cnpj, local, nome, uid, id;

  StoreEntity({
    required this.id,
    required this.uid,
    required this.nome,
    required this.categoria,
    required this.cnpj,
    required this.local,
  });
}
