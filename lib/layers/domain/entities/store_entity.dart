class StoreEntity {
  String categoria, cnpj, local, nome, uid;

  StoreEntity({
    required this.uid,
    required this.nome,
    required this.categoria,
    required this.cnpj,
    required this.local,
  });
}
